# 登录/农场重复功能合并 + 数据继承修复 + 实时同步

## 问题解决
1. **farm 中继承 login 数据失败**  
   - 原因：LOGIN_UID_KEY / LOGIN_NAME_KEY 在 login/LoginMain.ts 和 farm/GameRoot.ts 各自硬编码重复定义。
   - 修复：统一导出到 `login/Net.ts`，farm 直接 `import { LOGIN_UID_KEY, LOGIN_NAME_KEY } from '../login/Net'`

2. **farm 操作时后台数据实时验证和更新**  
   - 新增 `UserApi.saveAndSync()`：先 POST 保存 → 立即 GET 拉取服务端最新权威数据。
   - `GameRoot.persist()` 改为 async，保存后用后端返回数据**覆盖本地** `player.gold` + `inventory`。
   - 操作后自动刷新 HUD + 已打开的 Backpack/Shop 面板。

3. **重复功能文件合并，减少文件数量**
   - 删除 `login/ServerConfig.ts`（SERVER 配置）
   - 删除整个 `farm/net/UserApi.ts` + `farm/net/` 目录
   - **全部合并到 `login/Net.ts`**（单一网络出口）：
     - SERVER 配置
     - LOGIN_*_KEY
     - Http 请求封装（支持 wx + xhr）
     - UserApi + RemoteUserState + saveAndSync
     - toStack / toRow 映射函数
   - 最终文件数：
     - 原：login/ 3个 + farm/ 11个 = ~14 个 .ts
     - 现：login/ 2个 + farm/ 10个 = **12 个 .ts**（减少 2 个核心文件 + 1 个目录）

## 当前目录结构（优化后）
login/
  LoginMain.ts          # 登录/注册（使用 Net 中的 Http + SERVER + keys）
  Net.ts                # ★ 唯一网络中心（Http + SERVER + LOGIN_KEYS + UserApi）

farm/
  GameRoot.ts           # 入口（从 login/Net 继承全部登录态 + API）
  config/ItemConfig.ts
  data/
    InventoryModel.ts
    ItemData.ts
    PlayerModel.ts
  ui/
    BackpackPanel.ts
    CellItem.ts
    ShopItem.ts
    ShopPanel.ts
    Toast.ts

## 关键改动点
- login/Net.ts 现在同时被 login/LoginMain 和 farm/GameRoot 引用。
- farm 操作（买/卖） → onChanged = persist() → saveAndSync() → 后端验证 → 本地覆盖。
- 以后改服务器地址只改 login/Net.ts 一处。
- 以后想扩展实时同步逻辑也只改 Net.ts 的 UserApi。

## 验证建议
1. 登录 → 跳转 farm（检查 localStorage 是否正确传递）
2. farm 内买/卖物品 → 观察金币 & 背包是否立即刷新 + 控制台无报错
3. 刷新页面或重启后 farm 仍能从后端恢复最新数据
