# 背包 & 商店面板 — Cocos 编辑器搭建指南（精确尺寸 · 对照网页 1:1 还原）

> 画布设计分辨率 **1280 × 720**，所有尺寸单位均为 **px**。

---

## 一、HUD 层（场景根级节点）

| 节点 | 类型 | 尺寸 (W×H) | 位置 | 样式 |
|---|---|---|---|---|
| **GoldLabel** | Label | 自适应 | 左上 `(18, 18)` 距左上角 | 字号 **18**，粗体 800，色 `#ffe9b0 (255,233,176)`<br>背景：Sprite 圆角 **22**，色 `rgba(20,30,12,0.62)`，边框 **2** 色 `#ffe9b0`<br>Padding **8 × 16** |
| **OpenShopBtn** | Button | **92 × 92** | 左下 `left=36, bottom=36` | 圆形 `border-radius=46`，边框 **4** 色 `#ffe9b0`<br>背景渐变 `#f0a93d → #cf7a1e`<br>图标 🛒 字号 **30** + "商店" 字号 **16** 粗体 |
| **OpenBackpackBtn** | Button | **92 × 92** | 右下 `right=36, bottom=36` | 圆形 `border-radius=46`，边框 **4** 色 `#ffe9b0`<br>背景渐变 `#7bc043 → #4e8a2a`<br>图标 🧺 字号 **30** + "背包" 字号 **16** 粗体 |
| **Toast** | Node (挂 Toast.ts) | 自适应 | 顶部居中 `top=90, center-x` | 字号 **16**，粗体，色 `#ffe9b0`<br>背景色 `rgba(20,30,12,0.92)`，边框 **2** 色 `#ffe9b0`<br>圆角 **24**，Padding **10 × 22** |

---

## 二、BackpackPanel 完整尺寸

### 整体结构与尺寸

```
BackpackPanel (挂载 BackpackPanel.ts)
└── Overlay ── 1280 × 720 (全屏遮罩)
    └── Panel ── 680 × 543 (主面板)
        ├── Header ────── 674 × 66
        ├── Toolbar ───── 674 × 57
        ├── ScrollView ── 674 × 372
        └── Footer ────── 674 × 42
```

### 2.1 Overlay（全屏遮罩）— ⚠️ 用 Sprite，不要用 Mask

> **Mask 是裁剪组件，会把超出范围的子节点（Panel）裁掉，不能用于遮罩！**
> 正确做法：挂 **Sprite** 组件（用纯白 1×1 单色图），通过 Color + Alpha 实现半透明遮挡。

| 属性 | 值 |
|---|---|
| 组件 | **Sprite**（Type = Simple，SpriteFrame = 纯白 1×1 单色图） |
| 尺寸 | **1280 × 720**（Widget 上下左右 = 0，铺满画布） |
| 锚点 | `(0.5, 0.5)` |
| Sprite Color | `(8, 20, 10, 140)` 即 `#08140A` alpha **140/255 ≈ 55%** |
| 功能 | 点击 Overlay 空白处关闭面板（脚本自动处理，不需手动绑事件） |

### 2.2 Panel（主面板）— 圆角 + 边框实现方式（Cocos 3.8+）

| 属性 | 值 |
|---|---|
| 尺寸 | **680 × 543** |
| 锚点 | `(0.5, 0.5)`，位置 `(0, 0)` 相对 Overlay 居中 |
| 圆角 | **18** |
| 边框 | **3**，色 `#d9b56a (217,181,106)` |
| 背景 | 渐变 `#5a7a33 (90,122,51)` → `#38521f (56,82,31)` |

#### ⭐ 圆角 + 边框在 Cocos 3.8+ 的三种实现方式

**方式一：九宫格切图（推荐，最通用）**

用 PS/Figma 导出一张带圆角+边框的 `.png` 底图（如 64×64，圆角 18，边框 3，金色边+绿色内），然后：

1. Panel 节点挂 **Sprite** 组件
2. SpriteFrame 设为这张底图
3. Sprite → **Type = Sliced**（九宫格拉伸）
4. 在 SpriteFrame 资源的属性面板里设 **Border**（上下左右各 = 圆角值 **18**）
5. 这样不管 Panel 拉多大，四角圆角和边框都不会变形

> 九宫格底图建议尺寸：`64 × 64` 或 `128 × 128`，中间区域纯色即可。
> 如果需要渐变背景，底图高度做大一些（如 128 高），渐变画在底图里。

**方式二：双层 Sprite 叠加（不用切图）**

用两个 Sprite 节点叠在一起，外层做边框，内层做背景：

```
Panel (Node，680 × 543)
├── BorderSprite  (Sprite，680 × 543，色 #d9b56a 金色，做边框)
│     └── Sprite 类型 = Simple，用引擎内置 default_sprite_splash
│     └── 如需圆角：使用圆角白色底图九宫格
└── BgSprite      (Sprite，674 × 537，色 #5a7a33 绿色，做背景)
      └── 尺寸 = 父级 - 边框宽度×2 = 680-6=674, 543-6=537
      └── 如需圆角：圆角 = 18-3 = 15（内圆角 = 外圆角 - 边框宽度）
```

**方式三：Graphics 组件绘制（纯代码，不需要图片资源）**

在 Panel 节点上挂 **Graphics** 组件，用脚本绘制圆角矩形 + 边框：

```typescript
// 在 BackpackPanel.ts 或单独脚本中
import { Graphics, Color } from 'cc';

function drawRoundedPanel(g: Graphics, w: number, h: number) {
    const borderWidth = 3;
    const radius = 18;

    // 1) 先画金色边框（外层圆角矩形）
    g.fillColor = new Color(217, 181, 106);  // #d9b56a
    g.roundRect(-w/2, -h/2, w, h, radius);
    g.fill();

    // 2) 再画绿色背景（内层圆角矩形，缩进 border 宽度）
    g.fillColor = new Color(90, 122, 51);    // #5a7a33
    g.roundRect(
        -w/2 + borderWidth,
        -h/2 + borderWidth,
        w - borderWidth * 2,
        h - borderWidth * 2,
        radius - borderWidth   // 内圆角 = 18-3 = 15
    );
    g.fill();
}
```

> Graphics 方式最灵活，但不支持渐变填充。如果需要渐变背景，建议用方式一。

#### 各节点圆角 + 边框参数汇总

| 节点 | 外尺寸 | 圆角 | 边框宽 | 边框色 | 内尺寸 | 内圆角 |
|---|---|---|---|---|---|---|
| **Panel** | 680×543 | **18** | **3** | `#d9b56a (217,181,106)` | 674×537 | 15 |
| **CloseBtn** | 36×36 | **9** | **2** | `#ffb3b3 (255,179,179)` | 32×32 | 7 |
| **Tab 按钮** | ~60×36 | **18** | **2** | `rgba(217,181,106,0.55)` | ~56×32 | 16 |
| **SortBtn** | min-w 64 | **14** | **2** | `rgba(217,181,106,0.45)` | — | 12 |
| **CellItem** | 96×96 | **12** | **2** | `#b89b63 (184,155,99)` | 92×92 | 10 |
| **ShopItem** | 96×96 | **12** | **2** | `#b89b63 (184,155,99)` | 92×92 | 10 |
| **SellBtn** | 自适应 | **8** | **1** | `#d9b56a (217,181,106)` | — | 7 |
| **BuyBtn** | ~56×24 | **12** | **2** | `#4e8a2a (78,138,42)` | — | 10 |
| **GoldHUD** | 自适应 | **22** | **2** | `#ffe9b0 (255,233,176)` | — | 20 |
| **Toast** | 自适应 | **24** | **2** | `#ffe9b0 (255,233,176)` | — | 22 |
| **HUD 圆形按钮** | 92×92 | **46**(圆) | **4** | `#ffe9b0 (255,233,176)` | 84×84 | 42 |

### 2.3 Header

| 属性 | 值 |
|---|---|
| 尺寸 | **674 × 66**（Panel 宽 680 − 边框 3×2 = 674） |
| Layout 方向 | 横向，两端对齐 |
| Padding | 上下 **14**，左右 **22** |
| 底部分割线 | **2**，色 `rgba(217,181,106, 0.5)` |

| 子节点 | 尺寸 | 样式 |
|---|---|---|
| **TitleLabel** | 自适应 | 字号 **22**，粗体 800，字距 **4**<br>色 `#ffe9b0 (255,233,176)`<br>文字：`"农 场 背 包"` |
| **CloseBtn** | **36 × 36** | 圆角 **9**，边框 **2** 色 `#ffb3b3 (255,179,179)`<br>背景色 `#5a2733 (90,39,51)`<br>Label: `"×"`，字号 **20**，白色 |

### 2.4 Toolbar

| 属性 | 值 |
|---|---|
| 尺寸 | **674 × 57** |
| Layout 方向 | 横向，两端对齐（左=Tabs，右=Sort） |
| Padding | 上下 **10**，左右 **18** |
| 子间距 gap | **12** |
| 背景色 | `rgba(0,0,0, 0.22)` |
| 底部分割线 | **1**，色 `rgba(217,181,106, 0.35)` |

#### Tabs 区（左侧，横向 Layout，gap **8**）

| 子节点 | 尺寸 | 样式 |
|---|---|---|
| **TabAll** | 自适应（约 **60 × 36**） | 圆角 **18**，边框 **2** 色 `rgba(217,181,106, 0.55)`<br>Padding 上下 **7**，左右 **16**<br>Label: `"全部"`，字号 **14**<br>默认色 `#f3e8cf`，激活背景 `#d9b56a`+文字 `#3a2a12`+粗体 |
| **TabSeed** | 同上 | Label: `"种子"` |
| **TabFruit** | 同上 | Label: `"果实"` |
| **TabFert** | 同上 | Label: `"化肥"` |

#### Sort 区（右侧，横向 Layout，gap **8**）

| 子节点 | 尺寸 | 样式 |
|---|---|---|
| **SortLabel** | 自适应 | Label: `"排序"`，字号 **12**，色 `#d7c79a (215,199,154)` |
| **SortTime** | 最小宽 **64**，高自适应 | 圆角 **14**，边框 **2** 色 `rgba(217,181,106, 0.45)`<br>Padding 上下 **6**，左右 **12**<br>Label: `"时间 ↓"`，字号 **13**<br>激活：背景 `rgba(217,181,106, 0.28)`+边框 `#ffe9b0`+文字 `#ffe9b0`+粗体 |
| **SortName** | 同上 | Label: `"名称"`，字号 **13** |

### 2.5 ScrollView

| 属性 | 值 |
|---|---|
| 尺寸 | **674 × 372** |
| 内边距 Padding | **18**（四边） |

#### Content（Grid Layout）

| 属性 | 值 |
|---|---|
| Layout Type | Grid |
| 列数 ConstraintCount | **6** |
| 间距 SpacingX / SpacingY | **12** |
| 格子尺寸 CellSize | **96 × 96**（正方形） |

### 2.6 Footer

| 属性 | 值 |
|---|---|
| 尺寸 | **674 × 42** |
| Layout 方向 | 横向，两端对齐 |
| Padding | 上下 **11**，左右 **22** |
| 顶部分割线 | **2**，色 `rgba(217,181,106, 0.3)` |

| 子节点 | 样式 |
|---|---|
| **FooterLabel** | 字号 **13**，色 `#efe2c0 (239,226,192)`<br>文字：`"共 0 件物品"`（脚本动态更新） |
| **HintLabel** | 字号 **13**，色 `#c9b484 (201,180,132)`<br>文字：`"点格子看详情 · 点「卖💰」出售 · 可上下滑动"` |

---

## 三、ShopPanel 完整尺寸

### 整体结构与尺寸（与背包相同框架，内容不同）

```
ShopPanel (挂载 ShopPanel.ts)
└── Overlay ── 1280 × 720 (全屏遮罩)
    └── Panel ── 680 × 543 (主面板)
        ├── Header ────── 674 × 66
        ├── Toolbar ───── 674 × 57
        ├── ScrollView ── 674 × 372
        └── Footer ────── 674 × 42
```

### 3.1 Overlay — 同背包，**1280 × 720**，Sprite Color `(8,20,10,140)`，**不要用 Mask**

### 3.2 Panel — 同背包，**680 × 543**

### 3.3 Header

| 子节点 | 尺寸 | 样式 |
|---|---|---|
| **TitleLabel** | 自适应 | 同背包，文字改为 `"农 场 商 店"` |
| **CloseBtn** | **36 × 36** | 同背包 |

### 3.4 Toolbar

| 属性 | 值 |
|---|---|
| 尺寸 | **674 × 57** |
| 布局 | 同背包（横向两端对齐） |

#### Tabs 区（左侧，横向 Layout，gap **8**）— 只有 2 个 Tab

| 子节点 | 样式 |
|---|---|
| **TabSeed** | 同背包 Tab 样式，Label: `"种子"` |
| **TabFert** | 同背包 Tab 样式，Label: `"化肥"` |

#### 右侧 — ⚠️ **不是排序按钮**，是固定提示文字

| 子节点 | 样式 |
|---|---|
| **InfoLabel** | Label: `"仅售 种子 / 化肥"`，字号 **12**，色 `#d7c79a (215,199,154)` |

### 3.5 ScrollView — 同背包，**674 × 372**

#### Content（Grid Layout）— 同背包参数

| 属性 | 值 |
|---|---|
| 列数 | **6** |
| 间距 | **12** |
| 格子 | **96 × 96** |

### 3.6 Footer

| 子节点 | 样式 |
|---|---|
| **FooterLabel** | 字号 **13**，色 `#efe2c0`<br>文字：`"金币：500 💰  ·  在售 21 种"`（脚本动态更新） |
| **HintLabel** | 字号 **13**，色 `#c9b484`<br>文字：`"点「购买」放入背包 · 售价=回收价×2"` |

---

## 四、CellItem Prefab（背包格子）精确尺寸

| 属性 | 值 |
|---|---|
| 整体尺寸 | **96 × 96**（正方形） |
| 背景 Sprite | 渐变 `#f5ecd4 (245,236,212)` → `#e6d4ab (230,212,171)` |
| 边框 | **2**，色 `#b89b63 (184,155,99)` |
| 圆角 | **12** |

| 子节点 | 位置 | 尺寸 | 样式 |
|---|---|---|---|
| **NameLabel** | 顶部居中，距顶 **5** | 宽=父级，高自适应 | 字号 **11**，粗体 600，色 `#5b4422 (91,68,34)`<br>水平居中，单行截断 ellipsis<br>左右 Padding **4** |
| **IconSprite** | 正中心 | **36 × 36** | 居中对齐 |
| **Foot** | 底部，左右铺满 | 宽=父级，高自适应 | 横向 Layout，两端对齐<br>Padding **4 × 6** |
| ├ **CountLabel** | Foot 内左 | 自适应 | 字号 **13**，粗体 700，色 `#3a2a12 (58,42,18)` |
| └ **SellBtn** | Foot 内右 | 自适应 | 字号 **11**，粗体 700，色 `#8a4b13 (138,75,19)`<br>背景 `rgba(255,233,176, 0.75)`<br>边框 **1** 色 `#d9b56a`<br>圆角 **8**，Padding **1 × 6** |

---

## 五、ShopItem Prefab（商店物品卡）精确尺寸

| 属性 | 值 |
|---|---|
| 整体尺寸 | **96 × 96**（正方形） |
| 背景 Sprite | 渐变 `#f5ecd4` → `#e6d4ab`（同 CellItem） |
| 边框 | **2**，色 `#b89b63` |
| 圆角 | **12** |
| Layout 方向 | **纵向**居中，gap **3**，Padding **6** |

| 子节点 | 尺寸 | 样式 |
|---|---|---|
| **IconSprite** | **32 × 32** | 居中 |
| **NameLabel** | 自适应 | 字号 **12**，粗体 600，色 `#5b4422 (91,68,34)`，居中 |
| **PriceLabel** | 自适应 | 字号 **13**，粗体 700，色 `#3a2a12 (58,42,18)`<br>⚠️ 金币不足时变红 `#c0392b (192,57,43)` |
| **ResaleLabel** | 自适应 | 字号 **10**，色 `#8a7a55 (138,122,85)` |
| **BuyBtn** | 自适应（约 **56 × 24**） | 字号 **13**，粗体 700，白色<br>背景 `#6cbf4e (108,191,78)`<br>边框 **2** 色 `#4e8a2a (78,138,42)`<br>圆角 **12**，Padding **4 × 16**<br>⚠️ 金币不足时 disabled + 灰色 `#b9b59f` |

---

## 六、完整颜色对照表

| 用途 | 网页 CSS | Cocos Color (R,G,B,A) |
|---|---|---|
| 遮罩背景 | `rgba(8,20,10,.55)` | `(8, 20, 10, 140)` |
| 面板背景(上) | `#5a7a33` | `(90, 122, 51)` |
| 面板背景(下) | `#38521f` | `(56, 82, 31)` |
| 面板边框 | `#d9b56a` | `(217, 181, 106)` |
| Header 底线 | `rgba(217,181,106,.5)` | `(217, 181, 106, 128)` |
| 标题文字 | `#ffe9b0` | `(255, 233, 176)` |
| 关闭按钮背景 | `#5a2733` | `(90, 39, 51)` |
| 关闭按钮边框 | `#ffb3b3` | `(255, 179, 179)` |
| Toolbar 背景 | `rgba(0,0,0,.22)` | `(0, 0, 0, 56)` |
| Toolbar 底线 | `rgba(217,181,106,.35)` | `(217, 181, 106, 89)` |
| Tab 默认边框 | `rgba(217,181,106,.55)` | `(217, 181, 106, 140)` |
| Tab 默认文字 | `#f3e8cf` | `(243, 232, 207)` |
| Tab 激活背景+边框 | `#d9b56a` | `(217, 181, 106)` |
| Tab 激活文字 | `#3a2a12` | `(58, 42, 18)` |
| 排序 label | `#d7c79a` | `(215, 199, 154)` |
| SortBtn 默认边框 | `rgba(217,181,106,.45)` | `(217, 181, 106, 115)` |
| SortBtn 激活背景 | `rgba(217,181,106,.28)` | `(217, 181, 106, 71)` |
| SortBtn 激活边框+文字 | `#ffe9b0` | `(255, 233, 176)` |
| 格子背景(上) | `#f5ecd4` | `(245, 236, 212)` |
| 格子背景(下) | `#e6d4ab` | `(230, 212, 171)` |
| 格子边框 | `#b89b63` | `(184, 155, 99)` |
| 物品名文字 | `#5b4422` | `(91, 68, 34)` |
| 数量文字 | `#3a2a12` | `(58, 42, 18)` |
| 出售按钮文字 | `#8a4b13` | `(138, 75, 19)` |
| 出售按钮背景 | `rgba(255,233,176,.75)` | `(255, 233, 176, 191)` |
| 出售按钮边框 | `#d9b56a` | `(217, 181, 106)` |
| 购买按钮背景 | `#6cbf4e` | `(108, 191, 78)` |
| 购买按钮边框 | `#4e8a2a` | `(78, 138, 42)` |
| 价格买不起 | `#c0392b` | `(192, 57, 43)` |
| 购买 disabled | `#b9b59f` | `(185, 181, 159)` |
| 回收价文字 | `#8a7a55` | `(138, 122, 85)` |
| Footer 正文 | `#efe2c0` | `(239, 226, 192)` |
| Footer 提示 | `#c9b484` | `(201, 180, 132)` |
| Footer 顶线 | `rgba(217,181,106,.3)` | `(217, 181, 106, 77)` |
| HUD 金币文字 | `#ffe9b0` | `(255, 233, 176)` |
| HUD 金币背景 | `rgba(20,30,12,.62)` | `(20, 30, 12, 158)` |
| 商店按钮(上) | `#f0a93d` | `(240, 169, 61)` |
| 商店按钮(下) | `#cf7a1e` | `(207, 122, 30)` |
| 背包按钮(上) | `#7bc043` | `(123, 192, 67)` |
| 背包按钮(下) | `#4e8a2a` | `(78, 138, 42)` |
| HUD 按钮边框 | `#ffe9b0` | `(255, 233, 176)` |
| Toast 背景 | `rgba(20,30,12,.92)` | `(20, 30, 12, 235)` |
| Toast 边框+文字 | `#ffe9b0` | `(255, 233, 176)` |

---

## 七、@property 绑定速查

### BackpackPanel.ts

| @property | 类型 | 绑定到 |
|---|---|---|
| `overlay` | Node | **Overlay** (1280×720 遮罩) |
| `panel` | Node | Overlay > **Panel** (680×543) |
| `titleLabel` | Label | Header > **TitleLabel** |
| `closeBtn` | Button | Header > **CloseBtn** (36×36) |
| `tabNodes[0~3]` | Node[] | Tabs > **TabAll / TabSeed / TabFruit / TabFert** |
| `sortLabel` | Label | Sort > **SortLabel** |
| `sortNodes[0~1]` | Node[] | Sort > **SortTime / SortName** |
| `scrollView` | ScrollView | **ScrollView** 组件 |
| `content` | Node | ScrollView > view > **Content** |
| `cellPrefab` | Prefab | **CellItem** 预制体 (96×96) |
| `footerLabel` | Label | Footer > **FooterLabel** |
| `hintLabel` | Label | Footer > **HintLabel** |
| `goldLabel` | Label | Footer > **GoldLabel** (可选) |

### ShopPanel.ts

| @property | 类型 | 绑定到 |
|---|---|---|
| `overlay` | Node | **Overlay** (1280×720 遮罩) |
| `panel` | Node | Overlay > **Panel** (680×543) |
| `titleLabel` | Label | Header > **TitleLabel** |
| `closeBtn` | Button | Header > **CloseBtn** (36×36) |
| `tabNodes[0~1]` | Node[] | Tabs > **TabSeed / TabFert** |
| `infoLabel` | Label | Toolbar > **InfoLabel** ("仅售 种子 / 化肥") |
| `scrollView` | ScrollView | **ScrollView** 组件 |
| `content` | Node | ScrollView > view > **Content** |
| `shopItemPrefab` | Prefab | **ShopItem** 预制体 (96×96) |
| `footerLabel` | Label | Footer > **FooterLabel** |
| `hintLabel` | Label | Footer > **HintLabel** |
| `goldLabel` | Label | Footer > **GoldLabel** (可选) |

---

## 八、注意事项

1. **Overlay 必须全屏**：挂 **Sprite** 组件（不是 Mask！），Widget 上下左右都设 0。Mask 会裁剪子节点导致 Panel 看不见。
2. **Panel 居中**：锚点 (0.5, 0.5)，位置 (0,0) 即相对 Overlay 居中。
3. **圆角 + 边框**：推荐九宫格切图（Sprite Type=Sliced），详见第 2.2 节的三种方案。
4. **Tab/Sort 按钮节点**需同时挂 `Button` + `Sprite`（颜色切换）+ 子 `Label`。Tab/Sort 的圆角边框同样用九宫格底图。
5. **Grid Layout**：Content 节点挂 Layout，Type=Grid，ConstraintCount=**6**，SpacingX=SpacingY=**12**。
6. **关闭事件自动绑定**：不需在编辑器里手动绑 click → 脚本 onLoad 自动处理。
7. **面板互斥自动注入**：GameRoot.onLoad 已处理，无需手动配置。
8. **分割线**（Header 底线 / Toolbar 底线 / Footer 顶线）：用一个 1 像素或 2 像素高的 Sprite 节点，宽度铺满父级（Widget left=0 right=0），Color 设对应半透明金色即可。
