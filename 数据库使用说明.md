# 数据库使用说明

> 本文档说明：**前端（Cocos Creator 3.8.7）每个功能模块在哪些地方用到了数据库里的哪些信息**。
> 后端为 Flask + MySQL，表结构详见《数据扩展说明.md》。

---

## 1. 整体数据流

```
┌─ login 场景（login/LoginMain.ts）────────────────────────────┐
│  注册 / 登录  ──POST /api/register | /api/login──▶  MySQL users   │
│  大区列表    ──GET  /api/regions─────────────────▶  （后端配置）  │
│                                                              │
│  登录成功：                                                    │
│    ① localStorage 写入 loggedInUserId（= users.id）           │
│    ② localStorage 写入 loggedInUsername（= users.username）   │
│    ③ director.loadScene('farm')  ┐                            │
└──────────────────────────────────┼────────────────────────────┘
                                    ▼
┌─ farm 场景（farm/GameRoot.ts）───────────────────────────────┐
│  读取 loggedInUserId 作为 userId                               │
│  GET  /api/game/state?user_id=xx ──▶ 读 users(金币) + player_inventory(背包) │
│                                                              │
│  背包购买（ShopPanel）/ 出售（BackpackPanel）→ 数据变化         │
│   ──POST /api/game/inventory──▶ 整包写回 player_inventory，同步 users.coins │
└──────────────────────────────────────────────────────────────┘
```

farm 场景与 login 场景**共用同一个服务器地址**：`login/ServerConfig.ts` 的 `SERVER.baseUrl`
（farm/net/UserApi.ts 默认使用它，改地址只需改这一个文件）。

---

## 2. `users` 表 —— 各字段在哪里被用到

| 字段 | 使用位置 | 用途 |
|---|---|---|
| `id` | `login/LoginMain.ts` 登录成功后写入 `loggedInUserId`；`farm/GameRoot.ts` 读取后作为 `userId` 调用 `/api/game/state`、`/api/game/inventory` | **login 场景传 farm 场景的钥匙**，一切玩家数据的查询条件 |
| `username` | 登录/注册接口入参；登录返回后存入 `loggedInUsername`；`GameRoot → PlayerModel.bindUser()` 作为昵称 | 账号标识与展示 |
| `password` | `LoginMain` 登录/注册时明文上送（EditBox 密码框） | 仅服务端校验，**前端不接触** `password_hash` / `salt` |
| `region` | `LoginMain` 大区选择；登录/注册时上送；大区列表来自 `GET /api/regions` | 账号所属大区 |
| `coins` | farm 场景的金币：`UserApi.fetchState()` 读取（兼容字段名 `coins`/`gold`，前端统一叫 gold）；买卖后 `saveInventory()` 携带最新 gold 回写 | farm 的购买/出售经济来源 |
| `level` | 登录接口返回字段之一 | 预留（farm 界面暂未展示，可做等级解锁作物） |
| `exp` | 同上 | 预留 |
| `diamonds` | 同上 | 预留 |
| `password_hash` / `salt` / `created_at` | 前端不读 | 仅服务端存档与校验 |

---

## 3. `player_inventory` 表 —— 各字段在哪里被用到

| 字段 | 使用位置 | 用途 |
|---|---|---|
| `user_id` | `UserApi.saveInventory()` 上送、`fetchState()` 查询条件 | 归属哪个玩家 |
| `item_id` | 映射为 `InventoryStack.id`（如 `seed_0`）；关联 `farm/config/ItemConfig.ts` 的静态定义 | 物品唯一标识 |
| `item_name` | `CellItem`（背包格子名称）/ `ShopItem`（商品名称）显示 | 展示名 |
| `icon` | `CellItem` / `ShopItem` 中 `resources.load('textures/items/<icon>')` | 图标（资源放 `resources/textures/items/`） |
| `category` | `BackpackPanel` 分类页签（全部/种子/果实/化肥）、`ShopPanel`（种子/化肥）过滤 | 分类筛选 |
| `count` | `InventoryModel.addItem / sellOne` 增减；格子显示 `x数量` | 数量 |
| `value` | 出售时 `sellOne()` 返还给金币（回收价） | 经济 |
| `acquired_at` | `BackpackPanel` 时间排序（映射为 `acquired` 时间戳） | 排序 |

**写入路径**：`BackpackPanel.sellItem()` / `ShopPanel.buy()` → 改 `InventoryModel` →
触发 `GameRoot` 注入的 `persist()` → `UserApi.saveInventory()` → 整包覆写该用户背包行。

**容错约定**：服务端可以只存 `(user_id, item_id, count)` 三样，
名称/图标/分类/回收价由前端 `farm/config/ItemConfig.ts`（物品单一来源）按 `item_id` 自动补齐
（`UserApi.ts` 的 `toStack()` 已做兼容）。

---

## 4. `farm_plots` 表 —— 预留（当前前端脚本未读取）

| 字段 | 计划用途 |
|---|---|
| `plot_index` | 地块编号 0–23 |
| `state` / `crop_type` / `planted_at` / `grow_stage` | 地块状态 / 作物 / 种植时间 / 生长阶段 |

对应后端接口 `GET /api/farm`、`POST /api/farm/action`（开发 / 浇水 / 施肥）。
做地块玩法时在 `farm/` 下新增模块即可，数据流沿用 GameRoot 的装配方式。

---

## 5. localStorage 约定（场景间唯一的数据通道）

| 键 | 写入处 | 读取处 | 内容 |
|---|---|---|---|
| `loggedInUserId` | `LoginMain.onLogin()` 成功 | `GameRoot.onLoad()` | `users.id`；后端没返回 id 时退化为 username |
| `loggedInUsername` | `LoginMain.onLogin()` 成功 | `GameRoot.onLoad()` | `users.username` |
| `game_remember` | `LoginMain` 勾选“记住账号” | `LoginMain.loadRemember()` | `{ acc, region }` JSON，预填登录页 |

退出登录（`GameRoot.backToLogin()`）会清除前两个键并 `director.loadScene('login')`。

---

## 6. 接口一览与数据格式

| 接口 | 方向 | 请求体 / 参数 | 响应 |
|---|---|---|---|
| `POST /api/login` | LoginMain | `{ username, password, region }` | `{ success, message, user: { id, username, region, coins, level, exp, diamonds, created_at } }` |
| `POST /api/register` | LoginMain | `{ username, password, region }` | 同上（新用户 coins=100, level/exp/diamonds=0） |
| `GET /api/regions` | LoginMain | 无 | `{ success, regions: [...] }` |
| `GET /api/game/state?user_id=<id>` | GameRoot | query: `user_id` | `{ success, data: { username, gold(或coins), inventory: [ { item_id, item_name, icon, category, count, value, acquired_at } ] } }` |
| `POST /api/game/inventory` | GameRoot | `{ user_id, gold, inventory: [ 同上 ] }` | `{ success }` |

说明：

- `UserApi.fetchState()` 对响应做了容错：`data` 包裹或扁平均可；金币 `gold` / `coins` 均识别。
- 新用户库里还没有背包行时，前端发一套初始背包（`ItemConfig.buildInitialInventory()`）并立即落库。
- 浏览器预览存在跨域，Flask 需开启 CORS（如 `flask-cors`）。

---

## 7. 运行前置清单

1. `login`、`farm` 两个场景都加入「项目 → 构建发布 → 场景列表」，否则 `director.loadScene` 找不到场景。
2. farm 场景中 `GameRoot` 组件需绑定：`backpack`、`shop`、`toast`、`goldLabel`、`openBackpackBtn`、`openShopBtn`（`backLoginBtn` 为可选返回登录按钮，`loginSceneName` 可改场景名）。
3. 物品图标放 `resources/textures/items/<icon>.png`（icon 名见 ItemConfig）。
4. 后端启动在 `SERVER.baseUrl`（默认 `http://127.0.0.1:8000`）；手机真机预览改成局域网 IP。
5. 可用测试账号：`admin/admin123`、`player001~003/123456`、`test/test123`。

---

## 8. 以后要扩展数据时，改动落点

| 需求 | 数据库 | 前端改动点 |
|---|---|---|
| 新玩家属性（如体力） | `users` 加列 | `login/Net.ts` 的 `UserInfo` 加字段 → `UserApi.fetchState()` 识别 → `PlayerModel` 存值 → HUD 展示 |
| 新物品 | 不用改表 | `farm/config/ItemConfig.ts` 的 RAW 数组加一行（id/value 自动生成），背包/商店自动同步 |
| 物品模板后台热更 | 新增 `items` 模板表 | `UserApi` 加拉取，ItemConfig 从“本地常量”改为“服务端下发” |
| 地块玩法 | 用 `farm_plots` | `farm/` 新增模块（数据模型 + UI），走 `/api/farm` 系列接口 |
